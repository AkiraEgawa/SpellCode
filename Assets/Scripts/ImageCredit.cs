using UnityEngine;

// This background is made by https://free-game-assets.itch.io/nature-landscapes-free-pixel-art

public class ImageCredit : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
